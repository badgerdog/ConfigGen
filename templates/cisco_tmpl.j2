conf t

hostname {{ host_name }}

interface Ethernet0/2
 ip address {{ intf2_address }} {{ intf2_mask }}
 shutdown
!
interface Ethernet0/3
 ip address {{ intf3_address }} {{ intf3_mask }}
 shutdown
!
end
!

copy running-config startup-config

